namespace AccountingProgram.Data.Models.Enums
{
    public enum Measure
    {
        Pcs = 100,
        Kg = 200,
        Gr = 300,
        Ltr = 400,
        Bag = 500,
        Box = 600,
        Bucket = 700
    }
}
